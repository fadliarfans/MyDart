/*

Inheritance = extends    -> membagikan behaviour kepada turunannya (hanya 1)
interface   = implements -> memaksakan behaviour dari suatu class kepada class yang menjadi implementnya (bisa banyak)
mixin       = with       -> menambahkan behaviour dari suatu mixin kepada class yang menggunakan with tersebut

Constructor    = (nama class itu sendiri) -> yaitu method yang dijalankan langsung setelah objek itu dideklarasikan

Parameter Underscore = (_) -> nilai yang masuk ke parameter tersebut akan diabaikan
method sync  = method ditungguin
method async = method yang diantepin atau ga akan ditungguin atau langsung ke perintah berikutnya
mixin tidak wajib di @override
interface wajib di @override

 */